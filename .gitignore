# WorkBuddy 项目数据（记忆/自动化/本地状态），不纳入版本控制
.workbuddy/

# Python 缓存
__pycache__/
*.pyc

# 项目虚拟环境（本地 .venv，不纳入版本控制）
.venv/

# 含敏感凭证（weibo_cookie / 数据库密码），不纳入版本控制
config.json

# 运行时生成的产物（可选：如需版本化报告可移除此行）
# 注：用 data/* 而非 data/，否则无法反选目录内的说明文件
# （被排除的目录 git 不再向下遍历，`!data/README.md` 会失效）
data/*
!data/README.md

# 妙想/东财查询输出（临时产物，不入库）
miaoxiang/
