# A股舆情操作指引 · 报告分类与手动运行规范

> 生效：2026-08-16 ｜ 更新：2026-08-17（简版模式取消）｜ 配套：`A股舆情操作指引Agent-功能说明书.md`
> 背景：原"工作日 21:00 自动任务"已取消，改为**用户每日手动触发**。本报告定义三类产出（早报/晚报/周报）+ 回测报告的触发时机、章节结构、命名与存放位置，作为手动运行时的统一标准，防止每次产出不一致。
> **2026-08-17 起：简版模式取消。除回测外一律跑全功能 9 章节报告。`a_stock_agent.py` 降为数据引擎（采集→JSON快照→入库），全功能报告由 `build_report_20260816.py` + WebSearch 实时拼装生成。**

---

## 一、触发方式（用户手动）

用户用自然语言触发，Agent 按对应类型执行：

| 用户指令 | 类型 | 建议执行时机 | 关键数据特征 |
|----------|------|--------------|--------------|
| "跑早报" / "盘前报告" | 早报 | 交易日 盘前 7:30–8:30 | 隔夜美股已收盘、A股未开盘 |
| "跑晚报" / "今日报告" / "盘后报告" | 晚报 | 交易日 盘后 15:30–16:30 | A股已收盘、当日资金/行情可得 |
| "跑周报" / "本周总结" | 周报 | 周末（周六/日） | 全周数据完整 |
| "跑回测" / "回测交叉验证" | 回测 | 任意（通常晚报/周报后） | 依赖已生成的报告 |

> 无明确类型时，默认按"晚报"理解（完整 9 章节）。

---

## 二、四类报告规格

### 1. 早报（盘前版）`早报-YYYYMMDD.html` → `reports/早报/`

不含当日收盘/当日主力资金/自选股当日表现（盘前无这些数据）。结构：

1. 核心结论（盘前基调）
2. 隔夜美股（三大指数 + 中概 + 全球人物马斯克/特朗普信号）
3. CPI 与宏观（最新已公布数据）
4. 宏观传导链监控（独立因子，**日元主导**：原油(触发)→日本加息→抛美债→FIMA→日元干预→套息平仓；隔夜节点值）
5. 地缘政治与原油（事件因子隔夜更新）
6. ETF 资金流向（**前一日**净申购/赎回）
7. 唐史主任长文分析 + 投星观点（最新）
8. 7 只自选股**盘前**操作指引（操作/逻辑/风险）
9. 今日盘前策略（仓位/大盘/板块/回避）
10. 免责声明

> 涨跌颜色、自选股 7 只、badge 语义与晚报一致（见功能说明书不变式）。

### 2. 晚报（盘后完整版）`晚报-YYYYMMDD.html` → `reports/晚报/`

= 现行权威 **9 章节完整结构**（功能说明书第四节），并补入当日实际数据：

- 新增/强化"附：主要指数（A股收盘）"章节，填入当日实盘涨跌幅、成交量。
- 第五章 ETF 资金流用**当日**数据；第七章共振信号纳入当日盘面验证。
- 第八章 7 只自选股指引附带**当日实际涨跌**与盘前判断是否兑现的对照（可选）。

### 3. 周报（周末版）`周报-YYYYMMDD.html` → `reports/周报/`

1. 核心结论（周度）
2. 本周市场回顾（主要指数周涨跌幅 + 成交）
3. 7 只自选股周度表现（涨跌/量能/相对大盘）
4. 共振信号周度统计（出现频次最高的主线）
5. 回测结果汇总（近 N 日方向命中率趋势、交叉验证一致率）
6. 宏观与传导链周度趋势（**日元主导链**：日本加息/抛美债/FIMA/日元/套息平仓 一周走向）
7. 下周展望与操作策略（仓位/主线/回避）
8. 免责声明

### 4. 回测报告 `回测报告-YYYYMMDD.html` → `reports/回测/`

由 `python backtest.py --all` 生成（解析 `reports/` 下所有早/晚/周报 → 新浪日 K 回测 1/3/5 日 → 独立技术信号交叉验证）。命名固定 `回测报告-YYYYMMDD.html`，**统一放入 `reports/回测/`**。

---

## 三、文件名与目录（防偏移）

```
reports/
  早报/     早报-YYYYMMDD.html
  晚报/     晚报-YYYYMMDD.html
  周报/     周报-YYYYMMDD.html
  回测/     回测报告-YYYYMMDD.html
  早期版本/  历史格式报告（如 9维度），含回测所需方向性判断，仍被递归扫描
```

- 文件名**带横线**（中文 + 横线在 shell 易错）→ 上传必须走 Python 子进程传绝对路径。
- 历史报告已迁移：9 章节报告 → `reports/晚报/`；`回测报告-20260816.html` → `reports/回测/`；`9维度` 等含方向性判断的早期格式 → `reports/早期版本/`（保留供回测）；纯早期简版 → `archive/legacy/`。
- 回测扫描根限定为 `reports/`（递归，含 `早期版本/`），**排除 `archive/`**，避免历史快照副本重复入库。

---

## 四、手动运行标准流程

### 早报 / 晚报 / 周报
0. **数据引擎**：先跑 `python a_stock_agent.py` 采集微博/全球/宏观/事件/技术/国家队/日本传导链/指数 → 产出 `data/fetched_YYYYMMDD.json` 快照（+ 可选入库）。简版 HTML 不再生成。
1. 按上表对应类型的章节结构，用 **WebSearch 拉取实时数据**（美股/CPI/ETF 等）+ 步骤0快照，拼装 9 章节报告（禁止用 `generate_full_report.py` 硬编码数据）。
2. 生成 HTML 写入对应 `reports/<类型>/<类型>-YYYYMMDD.html`。
3. 用资料库技能上传「我的文档」：
   - `connect_open_platform` 换 token（约 30 分钟有效）。
   - `import_html.py --token-stdin` 传**绝对路径**（Python 子进程）。
4. 晚报 / 周报 生成后，跑回测并上传（见下）。

### 回测（晚报/周报后执行）
```bash
python backtest.py --all
# 解析 reports/ 下所有报告 → 新浪日K回测 → reports/回测/回测报告-YYYYMMDD.html
```
回测报告生成后同样上传资料库「我的文档」。

---

## 五、与功能说明书的关系

- 本规范是"**何时出哪类报告、放哪**"的运行层标准；功能说明书是"**报告长什么样、数据从哪来、不可违背的硬规则**"的基线。
- 任何重构/每日运行，先守功能说明书的 9 条不变式，再按本规范选类型与目录。
- 早报/周报为晚报 9 章节的**裁剪/聚合版**，不得引入与不变式冲突的颜色、个股、badge 语义。
